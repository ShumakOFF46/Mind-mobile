/// Вкладка экрана профиля. Локальный UI-state (не персистится, не влияет
/// на данные) — переключение не ходит в API, только меняет, какая уже
/// загруженная секция отображается (см. profile_screen.dart).
enum ProfileTab { beauty, journal }
